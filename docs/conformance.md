# GRXCP Conformance Report

Generated by `grx-conform --markdown`. Do not hand-edit.

## Headline

**50 of 82 tracked CUDA Runtime entry points implemented (61%).**

The denominator is a curated surface, not the whole CUDA API — see the note at the top of `tools/common/cuda_api_table.inc` for what is counted and why. This is an **API coverage** number. It is not a conformance pass rate: nothing in this report executes a kernel.

| Category | Total | Mapped | Partial | Refused | Absent |
|---|---|---|---|---|---|
| device | 12 | 6 | 2 | 2 | 2 |
| error | 4 | 4 | 0 | 0 | 0 |
| event | 7 | 6 | 1 | 0 | 0 |
| graph | 3 | 0 | 0 | 0 | 3 |
| interop | 1 | 0 | 0 | 0 | 1 |
| ipc | 2 | 0 | 0 | 0 | 2 |
| launch | 9 | 1 | 5 | 0 | 3 |
| memory | 23 | 8 | 5 | 2 | 8 |
| module | 6 | 4 | 1 | 0 | 1 |
| profiler | 2 | 0 | 0 | 0 | 2 |
| stream | 10 | 4 | 3 | 0 | 3 |
| texture | 3 | 0 | 0 | 0 | 3 |

## Partial: works, with a caveat

| CUDA | GRXCP | Caveat |
|---|---|---|
| `cudaGetDeviceProperties` | `grxGetDeviceProperties` | fields the stack cannot source report -1 rather than a guess; numRegs and barrier count are unknown today |
| `cudaDeviceCanAccessPeer` | `grxDeviceCanAccessPeer` | always reports no: there is no peer path in hardware yet |
| `cudaHostAlloc` | `grxMallocHost` | the write-combining and portable flags are ignored; there is no hardware distinction to honour |
| `cudaMallocManaged` | `grxMallocManaged` | refused on backends whose command processor has no page-table walker, rather than returning a pointer that is not really managed |
| `cudaMemcpy` | `grxMemcpy` | a copy kind that contradicts the allocation map is rejected where CUDA leaves it undefined |
| `cudaMemcpy2D` | `grxMemcpy2D` | decomposed into row transfers; a strided descriptor path is a follow-up |
| `cudaMemcpy2DAsync` | `grxMemcpy2DAsync` | see cudaMemcpy2D |
| `cudaStreamCreate` | `grxStreamCreate` | ordering is correct; streams do not yet run concurrently because the command processor defaults to one queue |
| `cudaStreamCreateWithFlags` | `grxStreamCreateWithFlags` | see cudaStreamCreate |
| `cudaStreamCreateWithPriority` | `grxStreamCreateWithPriority` | priority reaches the queue but has no effect until multi-queue arbitration is wired |
| `cudaEventElapsedTime` | `grxEventElapsedTime` | measures with a host clock until the command processor writes back profiling timestamps; reported by eventTimingIsDeviceSide |
| `cudaLaunchKernel` | `grxLaunchKernel` | requires a registered parameter layout; a stub without one is refused rather than packed by guesswork |
| `cudaLaunchKernelExC` | `grxLaunchKernelEx` | cluster dimension, cooperative and priority attributes are honoured; the shared-memory carve-out attribute is refused because the hardware register does not exist |
| `cudaFuncGetAttributes` | `grxFuncGetAttributes` | numRegs reports -1 until the toolchain emits register metadata |
| `cudaOccupancyMaxActiveBlocksPerMultiprocessor` | `grxOccupancyMaxActiveBlocksPerMultiprocessor` | computed from the CTA dispatcher's warp, slot and shared-memory bounds; there is no register bound because the dispatcher does not gate admission on registers |
| `cudaOccupancyMaxPotentialBlockSize` | `grxOccupancyMaxPotentialBlockSize` | see cudaOccupancyMaxActiveBlocksPerMultiprocessor |
| `cuLaunchKernel` | `grxLaunchFunction` | takes one packed argument blob rather than CUDA's void** plus extra array |

## Refused: declared, returns `grxErrorNotSupported`

| CUDA | GRXCP | Why |
|---|---|---|
| `cudaDeviceGetAttribute` | `grxDeviceGetAttribute` | the attribute enum is CUDA-specific; use grxGetDeviceProperties |
| `cudaDeviceEnablePeerAccess` | `grxDeviceEnablePeerAccess` | needs NVLink-class remote decode on G100 or the coherent port on the c930 NPU |
| `cudaHostRegister` | `grxHostRegister` | pinning arbitrary existing host pages needs a driver path into the command processor host aperture that does not exist |
| `cudaHostUnregister` | `grxHostUnregister` | see cudaHostRegister |

## Absent: a port using these fails to compile

Failing at compile time is the point. A mystery at runtime would be worse.

| CUDA | Why |
|---|---|
| `cudaSetDeviceFlags` | the flags select spin/yield/blocking sync behaviour that has no analogue here |
| `cudaDeviceSetLimit` | no tunable device limits are exposed |
| `cudaMemcpy3D` | the descriptor struct pulls in pitched pointers and arrays; use grxMemcpy2D per slice |
| `cudaMemset2D` | no strided fill path yet |
| `cudaMallocPitch` | pitched allocation belongs with the strided copy path |
| `cudaMemPrefetchAsync` | managed-memory migration hints need a page-migration engine |
| `cudaMemAdvise` | see cudaMemPrefetchAsync |
| `cudaMemcpyToSymbol` | the .vxbin footer carries no device variable symbol table yet |
| `cudaMemcpyFromSymbol` | see cudaMemcpyToSymbol |
| `cudaGetSymbolAddress` | see cudaMemcpyToSymbol |
| `cudaStreamAddCallback` | host callbacks need a driver-side completion thread the runtime does not run |
| `cudaStreamBeginCapture` | graph capture belongs with CUDA graphs |
| `cudaStreamEndCapture` | see cudaStreamBeginCapture |
| `cudaFuncSetAttribute` | the attributes worth setting need the unified L1/shared carve-out register |
| `cudaFuncSetCacheConfig` | see cudaFuncSetAttribute |
| `cudaLaunchHostFunc` | see cudaStreamAddCallback |
| `cuModuleGetGlobal` | see cudaMemcpyToSymbol |
| `cudaGraphCreate` | CUDA graphs are deferred; the driver's batched command submission is the natural substrate when they land |
| `cudaGraphLaunch` | see cudaGraphCreate |
| `cudaGraphInstantiate` | see cudaGraphCreate |
| `cudaIpcGetMemHandle` | no cross-process device memory sharing |
| `cudaIpcOpenMemHandle` | see cudaIpcGetMemHandle |
| `cudaCreateTextureObject` | the texture units exist in hardware but are driven by the graphics path and not exposed to compute yet |
| `cudaDestroyTextureObject` | see cudaCreateTextureObject |
| `cudaMallocArray` | see cudaCreateTextureObject |
| `cudaGraphicsGLRegisterBuffer` | no graphics interop surface |
| `cudaProfilerStart` | use grx-prof; there is no in-process profiler control API |
| `cudaProfilerStop` | see cudaProfilerStart |

## Refusal verification

4 of 4 refused entry points verified to actually refuse at runtime.

## Behaviour a port will notice

| Property | State |
|---|---|
| Device | GRX-G100 (simx) |
| Warp shuffle | emulated through local memory, roughly an order of magnitude slower than a register shuffle |
| Event elapsed time | host clock; measures submission, not device execution |
| `__constant__` | lowered to read-only global memory; no broadcast bandwidth advantage |
| Managed memory | refused on this backend |

## What this report does not measure

Kernel execution. No kernel runs in *this report*, so nothing above says whether a kernel produces the right answer. That is covered separately: `ci/run_real.sh` compiles a kernel with the device toolchain, runs it on a real backend and checks the arithmetic.

Stream concurrency is still genuinely unproven. The command processor runs a single queue, so no test anywhere can currently fail because of a race between streams.
